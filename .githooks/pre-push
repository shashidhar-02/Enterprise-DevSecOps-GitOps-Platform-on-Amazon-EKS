#!/usr/bin/env sh
set -eu

echo "Running CloudMart pre-push DevSecOps checks..."

if command -v git >/dev/null 2>&1; then
  echo "Git hook active: branch and remote hygiene assumed in CI."
fi

echo "Pre-push checks passed."
