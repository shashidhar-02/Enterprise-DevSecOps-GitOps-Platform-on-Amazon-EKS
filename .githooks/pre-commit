#!/usr/bin/env sh
set -eu

echo "Running CloudMart pre-commit checks..."

if [ -d "apps/backend" ]; then
  (cd apps/backend && npm test)
fi

if [ -d "apps/frontend" ]; then
  (cd apps/frontend && npm run build)
fi

echo "Pre-commit checks passed."
