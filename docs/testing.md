# CloudMart Testing Guide

## Backend
- Run `npm test` in `apps/backend`.
- The current test suite validates the backend health endpoint.

## Frontend
- Run `npm run build` in `apps/frontend`.
- The build validates the React application and NGINX packaging.

## Local Stack
- Use `docker-compose.yml` from the repository root to run the app locally.
