package com.company.order.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaConfig {
    // Relying on Spring Boot Autoconfiguration via application.yml
}
